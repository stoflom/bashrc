# Define whence
whence ()
{ 
    local INPUTARG=`/usr/bin/type -ap $1`;
    if [ -x "${INPUTARG}" ]; then
        type -a "$1";
        /usr/bin/ls -al --color=auto "${INPUTARG}";
    else
        type -a "$1";
    fi
}
